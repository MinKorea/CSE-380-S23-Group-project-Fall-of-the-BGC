/**
 * An interface that defines a Path in our navigation system. For now, it's just
 * a skeleton interface. I suspect I'll want to add fields later.
 * 
 * @param T objects or points on the path. Most likely this will be Vec2 or 
 * number, but in theory you could create a path of arbitrary objects right?
 * 
 * @author PeteyLumpkins
 */
export default interface Path<T> {}